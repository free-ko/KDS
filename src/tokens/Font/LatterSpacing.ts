import type { LetterSpacingKey, LetterSpacingType } from './LatterSpacing.types'

const LetterSpacing: Record<LetterSpacingKey, LetterSpacingType> = {
  tight: '-0.3px',
  normal: '0px',
}
export default LetterSpacing
